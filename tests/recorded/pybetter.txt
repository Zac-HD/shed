not (x in y)
z == None
a == True
a == False
df[df.flag == True]  # doesn't break Pandas code

with a:
    # comment
    with b:
        pass

================================================================================

x not in y
z is None
a == True
a == False
df[df.flag == True]  # doesn't break Pandas code

with a:
    # comment
    with b:
        pass
