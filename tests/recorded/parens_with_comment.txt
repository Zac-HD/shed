(
    # "Pointless" parens with comments are preserved
    ["foo"].append("bar")
)

================================================================================

(
    # "Pointless" parens with comments are preserved
    ["foo"].append("bar")
)
